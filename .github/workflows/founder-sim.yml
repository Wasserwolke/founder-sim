name: Founder Sim - Test and Deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: founder-sim-pages
  cancel-in-progress: true

jobs:
  validate:
    name: Validate Founder Sim
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'
          cache: pip

      - name: Write change summary
        if: always()
        run: python scripts/change_report.py --github-summary

      - name: Install Python tooling
        run: python -m pip install -r requirements.txt

      - name: Validate Python scripts
        run: python -m compileall -q scripts

      - name: Validate JSON files
        shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' file; do
            echo "Checking $file"
            python -m json.tool "$file" >/dev/null
          done < <(find app/web asset_sources game/data -type f -name '*.json' -print0)

      - name: Validate JavaScript syntax
        shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' file; do
            echo "Checking $file"
            node --check "$file"
          done < <(find app/web/js app/web/mods -type f -name '*.js' -print0)

      - name: Validate web DOM contract
        shell: bash
        run: |
          set -euo pipefail
          html="app/web/index.html"
          for id in scene cameraStage environment objectLayer startupStatus backButton toast rainFx darknessFx; do
            grep -q "id=\"$id\"" "$html" || {
              echo "::error::Required DOM node #$id is missing from $html"
              exit 1
            }
          done
          grep -q '<img id="environment"' "$html" || {
            echo "::error::#environment must be an img element"
            exit 1
          }
          grep -q 'data-build=' "$html" || {
            echo "::error::index.html must expose a build token"
            exit 1
          }
          grep -q 'class="hud-stat"' "$html" || {
            echo "::error::HUD markup does not match the stylesheet contract"
            exit 1
          }
          for legacy_id in deskHotspots mapHotspots hotspotLayer; do
            if grep -q "id=\"$legacy_id\"" "$html"; then
              echo "::error::Legacy DOM node #$legacy_id must not be present"
              exit 1
            fi
          done

      - name: Validate web runtime graph and assets
        run: python scripts/asset_status.py

      - name: Smoke test legacy web runtime
        shell: bash
        run: |
          set -euo pipefail
          python -m http.server 8080 --directory app/web >/tmp/founder-sim-http.log 2>&1 &
          SERVER_PID=$!
          trap 'kill $SERVER_PID 2>/dev/null || true' EXIT
          sleep 2

          for path in \
            / \
            /assets/manifest.json \
            /locales/de.json \
            /data/objects.json \
            /data/scenes.json \
            /assets/environments/desk_room_starter/desk_room_starter_night_base.png \
            /assets/environments/storage_room_starter/storage_room_starter_base.png \
            /assets/map/city_map_starter/city_map_starter_base.png \
            /assets/atlases/prototype_atlas_001.png; do
            curl --fail --silent --show-error "http://127.0.0.1:8080$path" >/tmp/founder-sim-smoke.bin
            test -s /tmp/founder-sim-smoke.bin || exit 1
          done

          browser="$(command -v google-chrome || command -v chromium || command -v chromium-browser || true)"
          test -n "$browser" || { echo "::error::No Chromium/Chrome browser found"; exit 1; }

          "$browser" --headless --no-sandbox --disable-gpu --window-size=1600,1000 --virtual-time-budget=5000 --dump-dom \
            http://127.0.0.1:8080/ >/tmp/founder-sim-dom.html 2>/tmp/founder-sim-browser.log || {
              cat /tmp/founder-sim-browser.log
              exit 1
            }

          grep -q 'data-runtime="ready"' /tmp/founder-sim-dom.html || { echo "::error::Browser runtime did not reach ready state"; exit 1; }
          grep -q 'data-environment-ready="true"' /tmp/founder-sim-dom.html || { echo "::error::Office environment was not decoded"; exit 1; }
          grep -q 'data-scene="office"' /tmp/founder-sim-dom.html || { echo "::error::Initial environment is not office"; exit 1; }
          grep -q 'data-camera="overview"' /tmp/founder-sim-dom.html || { echo "::error::Initial camera is not overview"; exit 1; }
          grep -q 'data-object-id="foundersim:workstation_focus"' /tmp/founder-sim-dom.html || { echo "::error::Camera-focus placeholder missing"; exit 1; }

      - name: Install Godot 4.7.1 for CI
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/founder-sim-godot
          curl -L --fail --silent --show-error \
            'https://downloads.godotengine.org/?flavor=stable&platform=linux.64&slug=linux.x86_64.zip&version=4.7.1' \
            -o /tmp/founder-sim-godot/godot.zip
          unzip -q /tmp/founder-sim-godot/godot.zip -d /tmp/founder-sim-godot
          GODOT_BIN="$(find /tmp/founder-sim-godot -maxdepth 1 -type f -name 'Godot_*' -print -quit)"
          test -n "$GODOT_BIN"
          chmod +x "$GODOT_BIN"
          echo "GODOT_BIN=$GODOT_BIN" >> "$GITHUB_ENV"

      - name: Validate Godot project import and runtime
        shell: bash
        run: |
          set -euo pipefail
          EDITOR_LOG=/tmp/founder-sim-godot-editor.log
          RUNTIME_LOG=/tmp/founder-sim-godot-runtime.log
          PROTOTYPE_LOG=/tmp/founder-sim-godot-25d-runtime.log

          "$GODOT_BIN" --headless --editor --path "$GITHUB_WORKSPACE" --quit >"$EDITOR_LOG" 2>&1
          "$GODOT_BIN" --headless --path "$GITHUB_WORKSPACE" --quit-after 3 >"$RUNTIME_LOG" 2>&1
          "$GODOT_BIN" --headless --path "$GITHUB_WORKSPACE" res://game/scenes/prototypes/room_25d_prototype.tscn --quit-after 3 >"$PROTOTYPE_LOG" 2>&1

          cat "$EDITOR_LOG"
          cat "$RUNTIME_LOG"
          cat "$PROTOTYPE_LOG"

          if grep -E 'ERROR: res://|SCRIPT ERROR:|Failed loading resource:|Node not found:|Invalid access|Invalid assignment' "$EDITOR_LOG" "$RUNTIME_LOG" "$PROTOTYPE_LOG"; then
            echo "::error::Godot reported project/runtime errors even though the process returned zero."
            exit 1
          fi

  deploy:
    name: Deploy GitHub Pages legacy preview
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    needs: validate
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
      - name: Upload legacy web preview
        uses: actions/upload-pages-artifact@v4
        with:
          path: app/web
      - name: Deploy legacy web preview
        id: deployment
        uses: actions/deploy-pages@v4
